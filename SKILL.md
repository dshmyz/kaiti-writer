---
name: 开题报告
description: 撰写、审阅、修订北京航空航天大学（北航）研究生学位论文开题报告，默认 MPA/社科。用于"开题报告""MPA 论文""推荐开题方向""选题""还没想好论文题目"，以及起草各章（背景/意义/文献综述/研究方案）、按校格式排版、对照评审标准自查、搭大纲、查真实文献、控篇幅；完稿后可一站式出汇报 PPT（内置三套北航答辩模板）、演讲稿、答辩问答预测与多格式导出。写入时即去AI化。
---

# 北航学位论文开题报告 skill（入口）

本 skill 默认 **MPA/社科方向**（章节编号 `一、/（一）/1、`）；若用户为理工方向，编号改 `第一章/1.1/1.1.1`，并参照 `references/格式规范.md`。重资料已拆到 `references/` 与 `templates/`，按需 Read，勿全量加载。

> **先设 `$SKILL`**：下文所有命令都用 `"$SKILL/..."` 引用本技能自己的文件。开工时先定位一次本技能目录（就是本 SKILL.md 所在目录），例如：
> ```bash
> SKILL=~/.claude/skills/开题报告          # Claude Code 默认位置
> ls "$SKILL/scripts"                      # 应看到 4 个 .py，看不到就改成实际安装路径
> ```
> 其他智能体（Codex / Trae / Cursor 等）把本目录放在哪，`$SKILL` 就设成哪，命令原样可用。**跨智能体适配（工具名不同、无本地文件系统等）见 `README.md`「跨智能体使用」一节。**
>
> **工具名映射（非 Claude 环境照此代入，全文不再重复说明）**：
> - 下文说的 **`AskUserQuestion`** = "**一次性把 2–3 个选择题连同选项列给用户、等他回答再往下**"。没有这个工具就**直接在回复里写编号选项**（如 `A/B/C`，并说明"可自己输入其他答案"），然后**停下等用户回答**——**关键是"停下来等"，不是自己替用户选**。
> - **每个问题的选项须 ≥2 个真实选项，否则不要调用 AskUserQuestion。** 某个待确认事项若只有 1 条既定路径（如论文类型已由决策树判死、没有第二种选择），`options` 少于 2 会让工具直接报 `InputValidationError` 并连同整批问题一起全部发不出去；此时把它当**既定前提直接陈述**推进即可，只把同一批里确有 2+ 个选项的问题留下。细节见 `references/选题引导.md`「调 AskUserQuestion 的硬性要求」。
> - 下文说的 **`WebSearch`/`WebFetch`** = 任意联网检索/抓取能力。没有就走 `references/文献检索.md` 的 `curl` 直连开放 API（OpenAlex/Crossref）；**完全无网时不许编造文献**，按该文件第二级请用户提供文献。

## 何时使用
- 起草开题报告全文或某章（研究背景、文献综述、研究方案、实施计划等）。
- 审阅/自查是否符合北航规范与评审标准。
- **审阅或修订已有的一份开题报告**（用户给 .docx 路径或直接粘正文）→ 走 `references/审阅已有稿.md`。
- 据论文类型搭大纲、定正文结构。
- 询问时间节点、答辩时长、评分等级、查重比例等管理规定。
- **联网检索/查真实文献**、整理参考文献表、或写"（三）文献综述"章节（见 `references/文献检索.md`）。

## 开题报告 7 项必备内容（管理办法第三条，缺一不可）
开题题目；选题依据（意义/必要性/前沿性）；文献综述（深入调研→系统分析现状→指出不足→阐述趋势→引出拟研究问题）；研究方案（研究目标/内容/关键问题/方法/技术路线）；预期目标和成果；学位论文实施计划；主要参考文献。

## 标准结构（开题报告格式模板）
**官方 Word 模板 = skill 内置 `assets/templates/开题报告模板.docx`**（由官方 `开题报告格式2025.9.doc` 在 Word 中另存为 .docx 得到，保真）。模板内嵌封面 banner（42pt 华文行楷）、题目（32pt 加粗）、封面填写表格（作者姓名/专业名称·公共管理/指导教师/培养学院·公共管理学院）、目录 TOC 域、正文一~四章标题（style3/小四）、参考文献、附录、以及末尾"书写规范"说明段（生成时删除）。结构：封面 → 目录 →
一、研究背景与选题意义（(一)研究背景 (二)选题意义 (三)文献综述）
二、研究思路与框架（(一)研究思路 (二)研究框架（内容）(三)论文大纲，可加"核心概念与理论基础"）
三、研究方法与创新之处（(一)研究方法 (二)创新之处）
四、学位论文实施计划
**五、预期目标和成果**（官方模板**没有**这一节，但《管理办法》第三条要求；脚本会自动建节，见第 5 步）
→ 参考文献 → 附录（含访谈/问卷者须附提纲）。

## 论文类型决策树（先定类型再搭结构）
1. 题目含"以XX为例""个案"或聚焦单一典型事件 → **案例分析型**。
2. 题目含"调查""满意度""影响因素""实证"且有大样本 → **调研分析型**。
3. 题目针对某项政策/方案的制定、执行、评估、比较 → **政策分析型**。
4. 辨析某公共管理问题的成因与对策，无单一案例/政策主体 → **问题研究型**。
> 四类正文结构、写作要点与常见病、参考范文见 `references/论文类型详解.md`。

## 逐节质量闸（起草与自查标准）
> 字数由用户选择：起草前按 `references/篇幅档位.md` 确认篇幅档位（精简/标准/充实）或自定义总字数，并把该节字数目标作为篇幅约束（"写够不注水"）。
> **去AI化在写入时就执行**（中强度）：每写一段都按 `references/去AI化写作守则.md` 落笔，即"能删则删、能实则实"——不堆"技术赋能/多措并举/筑牢…根基"类空话，不用"进一步/深入/切实"充数，不写三句以上平行排比；句式长短错落、抽象处换具体。**不是写完再润色一遍，而是从第一稿就这样写**；保留学术书面语，不口语化。

- **研究背景**：由宏观到具体、由远及近，收束到研究对象；与意义有逻辑联系。
- **选题意义**：分理论/现实意义 + 前沿性必要性；忌与背景脱节、文不对题。
- **文献综述**：按主题/观点分类梳理（非罗列）；**指出研究空白并引出拟研究问题**；近 5 年文献占比过半；≥15 篇；述而有论，忌堆砌抄书。写此章前先按 `references/文献检索.md` 联网检索真实文献；**筛文献用 `references/ars-imports/source_quality_hierarchy.md`（来源分级），整理综述用 `references/ars-imports/literature_matrix_template.md`（文献矩阵，来源×主题交叉表）**。
- **研究思路**：文字 + 技术路线图，说明"问题→理论→方法→分析→结论"。**技术路线图用 `scripts/make_route_figure.py` 直接出图**（零新增依赖，Pillow + 系统宋体）：写 `route.json`（`nodes` 数组，元素为字符串=单框、为数组=同层并列分支），跑
  ```bash
  python "$SKILL/scripts/make_route_figure.py" --content route.json --output 路线图.png
  ```
  再用 `{"image": "路线图.png", "caption": "本研究技术路线图"}` 块嵌入。用户自己已有图就直接用他给的路径。**不要凭空声称"下图为技术路线"却没有图**。
- **研究框架/大纲**：各部分要解决什么问题、彼此关系；二级或三级提纲。
- **研究方法**：方法名 + 对象 + 工具 + 步骤 + 信度效度；定性与定量结合、三角互证；忌只列方法名。**含问卷/访谈的，按 `references/问卷与访谈提纲.md` 写清对象、抽样、拟发放规模、量表来源与拟检验方式，并把问卷/提纲全文附进 `appendix`**（模板明确要求）；开题阶段一律写「拟发放/计划访谈」，**不得出现回收份数、α 系数等尚未产生的数据**。
- **创新之处**：落到六方面之一（解释原则/视角、概念与框架重组、提问方式变革、分析方法改变、背景知识更新、逻辑关系转换）；忌"首次研究"空泛。
- **实施计划**：分阶段时间表，对齐节点（硕士第二学年11月底前开题；开题至答辩硕士≥5月、博士≥10月）。
- **参考文献**：GB/T 7714，详见 `references/参考文献著录.md`；须真实可核验，见 `references/文献检索.md`。

## 格式规范
字体字号、页面、图表、公式、目录、名词术语等见 `references/格式规范.md`。生成 docx 时据此执行。

## 管理规定与评审（自查对照）
- 时间：硕士第二学年11月底前完成开题；普通博士3年内、直博4年内；可申请延期1次（硕士≤6月、博士≤12月）。
- 答辩：公开，提前15天公布；硕士每人≥20分钟、博士≥40分钟；考核小组≥3人且≥1名校内本学科专家，博士组2/3以上成员具博导资格。
- 机会：学制内最多2次；首次未通过重开题（硕士1月后、博士2月后）；两次未通过者硕士终止培养、博士分流。
- 评分：百分制，优≥90/良80–89/中60–79/不及格<60；中以上通过；"中"须书面答复修改意见并修改、导师签字确认后留档。
- 题目变更：通过后原则上不改；实质内容变更须重开题；仅改题目需导师同意+考核/中期/评审/答辩小组之一建议。

## 查重阈值（送检前提醒）
检测结果按"去除本人文献文字复制比"：≤10%可直接评阅；10–20%修改后导师签字评阅；20–30%延期≥3月修改重检；30–40%专家鉴定，若无端修改≥6月重检，若有不端取消资格；>40%直接取消资格。异议5个工作日内书面申请。送检须终稿文本、与送审纸质版一致、原则上每篇限检1次。

## 优秀标准（对照提升）
1. 紧密结合公共管理实践，理论与现实意义重要；2. 前沿问题，实用或学术价值；3. 充分运用理论与方法解决问题；4. 主题明确论点清晰、论据充分可靠、分析全面推论严谨、方法先进可行、结论准确、文字精练引证规范；5. 创新六方面占其一。

## 工作流（Claude 执行）

### 先分叉：起草新稿，还是审阅已有稿？
**动手之前先判断用户处于哪种情形**，三条路入口不同，别混：

- **用户已经有一份稿子**（自己写的 / 上一轮生成的），要评一遍或改一遍 → **走 `references/审阅已有稿.md`**（读稿 `scripts/read_docx.py` → 对照自查清单逐条核 → 输出 ✅/⚠️ 与具体改法 → 问是否据此重建 docx）。跳过第 0 步与前 4 步。
- **导师退回的稿子 + 导师意见**（散乱批注、语音转文字、截图等）→ **走 `references/审阅已有稿.md`「导师反馈循环」一节**：先把散乱意见解析成按节分组的结构化清单，逐条执行修改，改完跑自查。跳过第 0 步与前 4 步。
- **用户还没有稿子**（含"没想好题目"）→ 从下面第 0 步开始。

判断不了就用 AskUserQuestion 问一句「你是要从零起草，还是已经有稿子要我审？导师有没有给过反馈意见？」，不要默认。

### 第 0 步：引导式提问，确定开题方向与篇幅偏好（用户在开题前往往没有成熟题目，须先引导）
在进入任何起草/搭大纲之前，先与该用户进行引导式对话，逐步收敛出一个**可写的开题方向**。默认以 MPA 为主要方向；若用户选择其他方向则随之切换。对话节奏如下：

> **硬闸：题目和大纲经用户确认之前，不要动笔写正文，也不要生成 docx。**
> 这条对**任何**题目都适用，包括看起来"很清楚、直接写就行"的。用户说"我题目定了你直接写"也不例外——至少要回读一遍题目、复核论文类型、确认篇幅档位，再进大纲。**看起来简单的题目恰恰是没说出口的假设最多的地方**：一个"社区治理研究"背后可能是问卷做不了、案例进不去、层级没定。这一步花两分钟，能省掉整篇重写。
>
> **题目过大先收窄，不要急着问细节。** 用户给的若是"我国基层治理现代化研究""数字政府建设研究"这类能写成专著的题目，**第一件事是告诉他这个题目开不了题**，并给出收窄方向（限定层级 / 限定对象 / 限定政策 / 限定时段），不要先去问访谈几人、问卷多少份——在一个必须拆的题目上打磨细节是白费的。收窄后的题目再走下面的常规节奏。
>
> **砍到只剩必要的**（YAGNI）：候选方向、研究内容、研究方法、理论框架都遵守"能少不多"。MPA 开题最常见的病不是写少了，是**堆**——堆三个理论、五种方法、四个案例地。多一个方法就多一份要交代的信效度，多一个理论就多一段要圆的自洽。删到"少了它论证就不成立"为止。

1. **第一问同时问清"学术方向 + 题目状态"**（一次 AskUserQuestion 两问，即 `references/选题引导.md` 第 1 轮）：方向**默认给"MPA 公共管理（推荐）/ 其他社科 / 理工"**——若选理工，章节编号改用 `第一章/1.1/1.1.1` 并参照 `references/格式规范.md`；题目状态问"已定题 / 有大致范围 / 完全没想好"。
2. **按题目状态分岔**：
   - **已定题** → 跳到下面第 5 条，复核研究层次与论文类型；若用户能一句话说清题目但细节不清楚（比如只说了"社区治理"没说哪个社区），先走 `references/选题引导.md` 第 2 轮攒素材再收束，不走三轮全流程。
   - **有范围 / 完全没想好** → **走 `references/选题引导.md` 第 2、3 轮**，那里有固定提问脚本与候选卡格式，照它执行，不要临场自拟问法。用户看完候选说"换一批"/"都不感兴趣"是正常流程，照 `选题引导.md`「换一批」一节处理，最多换两批；**不要问用户为什么不感兴趣**——直接换方向出新一批。**三轮 + 候选卡完成后，回到这里第 5 条继续收束**。
3. **候选卡的生成原则**：`选题引导.md` 第 2 轮里「你能拿到什么类型的材料」是决定性的一问——**MPA 选题死在"数据拿不到"的远多于死在"没创意"**。能拿一手材料/能访谈→案例分析型可行；能本人发放 ≥200 份问卷→调研分析型可行；只有公开资料→政策分析型或问题研究型。**候选须附论文类型判断**（用下面"论文类型决策树"），用户选了哪个方向后，**必须用决策树复核一遍**——用户说"以某街道为例"但候选卡标的是"政策分析型"，这里要纠正成"案例分析型"。
4. **列完候选必须给推荐意见**：明确说首选/次选/不建议各是哪个**并给理由**（数据成本、文献厚薄、风险集中在哪一点），别只列清单丢给用户挑——用户找你就是要这个判断。可顺带点明创新点落在六方面的哪一个，省得用户后面硬找新意。
   > **不需要 `brainstorming` 技能**：它的核心（定题前硬闸、题目过大先拆、带取舍的候选+明确推荐、YAGNI、分节确认、大纲自查）已全部移植进本技能，对应关系见 `references/选题引导.md` 末节。环境里有没有它都不影响，**不要去调用它**。
5. **收束：确认研究层次 + 论文类型 + 问卷/访谈意向 + 篇幅档位**（已定题的用户从第 2 条直接跳到这里，其余用户走完选题引导后也回到这里）。四个问题**一次性问清**（一次 AskUserQuestion 四问，不要分四轮）：
   - **研究层次**：聚焦哪个具体社区 / 街道 / 区县（题目里要不要"以某某为例"）
   - **论文类型**：用上面"论文类型决策树"复核（候选卡里已判过一次，这里再确认）
   - **要不要帮拟问卷 / 访谈提纲**（判为调研分析型、案例分析型，或用户答过"能发问卷/能访谈"，即命中）：选项「问卷 + 访谈提纲都要 / 只要其中一项 / 先不用，我自己写」，做法见 `references/问卷与访谈提纲.md`。**在这里问、不要等正文写完**——样本量与量表维度要写进「研究方法」一节，晚了得回头改。用户说不用就不做，但要提醒他附录这项还空着（模板要求含问卷/访谈者须附提纲）。**此步问过，起草时不再重复问。**
   - **篇幅档位**：按 `references/篇幅档位.md`，选项"精简(≈4000)/标准(≈6000)/充实(≈8000)"，用户也可选 Other 直接输入目标总字数；默认"标准"。据此换算各节字数目标，作为起草的篇幅约束。
6. 收束完成后，进入**起草流程**（下面紧接的部分）。

### 起草流程（第 0 步第 5 条收束完成后执行）

1. **定边界**：确认社科/理工方向与论文类型（用上面决策树）；若题目特征不清，用 AskUserQuestion 一次问清，勿反复追问。
2. **联网检索与整理文献**：写文献综述前，按 `references/文献检索.md` 用 WebSearch/WebFetch 检索真实文献，分类、去重、整理成 GB/T 7714 条目（≥15 篇、近 5 年过半），供文献综述引用与参考文献表使用。用户自备的文献一并纳入；**若检索真缺，按 `文献检索.md` 第二级让用户选择是否提供本地文献目录路径（可选，用户可不填）**。
3. **搭大纲**：以 `templates/开题报告大纲.md` 为内容草稿脚手架，据类型套 `references/论文类型详解.md` 对应结构，生成提纲请用户确认。**大纲交给用户之前，先自己过一遍四问**（照 `references/选题引导.md`「大纲自查」执行，发现问题当场改，不必回头再查一轮）：占位没填完的？前后自相矛盾的（如方法写问卷、样本却说访谈）？范围仍偏大写不完的？表述有两种读法的？
   > **大纲确认后用户要大改**（结构性调整，不是改几个措辞）→ 退回本步重新搭，把用户的修改意见逐条落实进新大纲再交确认。**最多退回两次**。两次还不过 → 停下来，把两轮修改意见逐条列出来，用 AskUserQuestion 让用户从中圈定哪些是必须改的、哪些可以先放——能走到这一步的用户通常信息充足但选择困难，帮他收敛比继续发散更有效。
4. **逐节起草（写入时即去AI化）**：按"逐节质量闸"产出正文，并**按第 0 步第 5 条用户选择的篇幅档位/自定义字数**执行各节字数目标（见 `references/篇幅档位.md`，写够不注水）；**落笔时同步执行 `references/去AI化写作守则.md`（中强度）——写进 content.json 的就是已去AI化的文字，不设"事后统一润色"环节**；文献综述须述而有论、引第 2 步检索的真实文献；方法须可操作；创新须落到六方面。**若第 0 步第 5 条已决定帮拟问卷/访谈提纲，先按 `references/问卷与访谈提纲.md` 生成量表题项和访谈问题，再写进内容——研究方法一节里要写清对象、抽样、拟发放规模、量表来源与拟检验方式，并把问卷/提纲全文附进 `appendix`。**
   > **一节一确认，别攒到最后**：写完**文献综述**和**研究方法**这两节各停一次，把这一节给用户看并问"这节方向对不对"。这两节是被导师退回的高发区（综述罗列不分类、方法只列名不可操作），且一旦方向错了，后面几节都要跟着改。其余节可连续写完再一并回显。每节篇幅按档位走，别为凑字数注水。
   > **去AI化中间扫描**：给用户看之前，先按 `references/去AI化写作守则.md` 扫一遍——有没有出现「进一步/深入/切实/多措并举/技术赋能/筑牢/赋能/新范式/深度融合」等高频 AI 句式？有没有三句以上平行排比？有没有抽象堆叠（"具有重要的理论意义和现实意义"）？**有就当场改掉再给用户看**，不要等自查才补。这是去AI化唯一的检查窗口——自查时只查格式与内容完整性，不再逐句审文字。
5. **生成 .docx（标准路径，仿 buaa-final-assignment 范式）**：把内容写成 `content.json`（格式见下），用内置脚本基于官方模板生成，**不重建文档、保留全部模板样式**：
   > **写 Python 处理中文 json / 文本时的编码护栏**（脚本质量）：`ensure_ascii=False` 是 **`json.dump()`** 的参数，不是 `open()` 的——写到文件用 `open(path,'w',encoding='utf-8')`，要保留中文可读则 `json.dump(obj, f, ensure_ascii=False)`；**绝不要写成 `open(..., 'w', ensure_ascii=False)`**（会直接抛 `TypeError: 'ensure_ascii' is an invalid keyword argument for open()`）。凡把中文内容（content.json、route.json 的节点文字、演讲稿正文、文献条目）落盘，一律显式 `encoding='utf-8'`，避免默认编码导致的中文乱码。
   ```bash
   python "$SKILL/scripts/build_from_template.py" \
     --template "$SKILL/assets/templates/开题报告模板.docx" \
     --content content.json \
     --output "开题报告-<题目>.docx"
   ```
   脚本做法：深拷贝模板段落 pPr、只换 run 文本（**不重建文档、保留全部样式**）；替换封面题目与封面表格填写位；在各节标题后插入正文段（宋体/TNR/小四、保留缩进行距）；替换参考文献条目；填附录；**删除末尾"书写规范"段及著录表**；保留模板图片（校徽）。用 **lxml**（非 stdlib ElementTree）解析序列化，原生保留 17+ 个命名空间前缀，避免 Word 提示"需修复"。`content.json` 格式见 `templates/内容示例-社区减负.json`：
   - `title`/`subtitle`：题目主/副标题；`subtitle` 为空则自动删副标题空行。
   - `cover`：`{"作者姓名":"...","专业名称":"...","指导教师":"...","培养学院":"..."}`，四项均可选；未给则保留模板默认（公共管理/公共管理学院）。
   - `content_by_section`：键为模板里已有的节标题文本（如`"（一）研究背景"`、`"研究框架（内容）"`、`"四、学位论文实施计划"`），值为该节内容数组，**按数组顺序依次插入**。数组元素可以是纯文本段落（字符串），也可以是下面四种**混排块**（对象），可自由穿插：
     | 块 | 写法 | 生成结果 |
     |---|---|---|
     | 表格 | `{"table":{"headers":[...],"rows":[[...]],"caption":"访谈对象分布"}}` | 学术三线表 + **表题在表上方**（五号宋体加粗居中，自动编号"表1/表2…"） |
     | 插图 | `{"image":"/abs/path/技术路线图.png","caption":"技术路线图","max_width_cm":14}` | 嵌入图片（按原始像素等比缩放，默认最宽 14cm 不超版心）+ **图题在图下方**（自动编号"图1/图2…"） |
     | 列表 | `{"list":["第一点","第二点"],"ordered":true}` | Word 真实自动编号列表；`ordered:true`→`1. 2. 3.`，省略或 false→`●` 项目符号 |
     | 脚注 | `{"text":"这是正文段落。","footnote":"注文内容，如资料来源说明。"}` | 该段落末尾加上标注号 + 页脚注文 |

     图/表题**编号全局连续**（跨节累加），符合 `references/格式规范.md`「表题在表上、图题在图下」。图片路径须为**已存在的本地绝对路径**，不存在则打印警告并跳过该块（不中断生成）。四类论文的 content.json 示例见 `templates/内容示例-*.json`（`-社区减负`=问题研究型且**演示四种混排块**、`-案例分析`、`-调研分析`、`-政策分析`）。**勿改键名**否则找不到节。
     > 示例文件带 `"_demo": true`：它们的封面、参考文献故意留 `〈XXX〉` 占位（换成像真的假文献会诱导用户直接交编造的引文），这个键让自检把占位警告标注成"示例预期如此"。**给用户建 content.json 时不要带这个键**，否则真占位也会被标成预期。
     > **`"五、预期目标和成果"` 是例外**：官方模板正文只有一~四章，没有这一节（模板里"预期"出现 0 次），但《管理办法》第三条把它列为 7 项必备之一。给这个键，脚本会**自动建节**——插在「参考文献：」之前，标题样式克隆「三、」（`pStyle=3` 故 F9 后自动进目录、四号字合规），重复运行不会建出第二个。模板原文写有「可按照导师建议进行调整」，故新增章节符合模板意图。**这是白名单里唯一可自动新建的节**，其他键名拼错仍会报「⚠️ 未找到节标题」。
   - `plan_table`（可选，兼容旧写法）：实施计划三线表，`{"headers":[...],"rows":[[...]],"caption":"..."}`；给了则在"四、"节末尾生成三线表。新写法建议直接在 `content_by_section["四、学位论文实施计划"]` 里用 `table` 块，便于控制表格与说明文字的前后顺序。
   - `refs`：参考文献条目数组（GB/T 7714，见 `references/参考文献著录.md`）。**写进 json 前先跑 `scripts/fix_refs.py` 做收尾核查**（自动去末尾结束符、清"待核验/占位"括号、统一半角标点），核查出"待补/待核验"的真实数据缺项时不要自己编，删占位连标点略去后**单独列清单让用户补真值**（参考 `references/参考文献著录.md`「写入前收尾核查」）。**待核验阶段可顺手在 docx 里就地用 `--mark-missing "6:发文字号待补;11:…"` 给参考文献缺项条目加红色提示**（content.json 保持干净），用户补完用 `--unmark-red` 清除即定稿。**正文里的"噪声红"**（模板目录缓存红字、误染红等）用 `--clear-noise-red` 清掉——它会保留以 `〈待确认` 开头的"确认红"、只删其余红，参考文献区不动（见「缺项的就地标注」）。
   - `appendix`：附录条目数组，**接受与 `content_by_section` 完全相同的四类混排块**（字符串/表格/插图/列表），所以问卷量表题应排成真表格。附录内的表/图另走一套「附表1 / 附图1」编号，不占正文那套连续号。
   脚本在正文区（精确匹配"开题报告"标题之后）查找节标题，不会误插到目录 TOC。生成后**Word 打开时会自动更新目录页码**（脚本置了 `updateFields`，弹"是否更新域"选是即可；若被关掉，仍可手动点目录按 F9）。**「五、预期目标和成果」「参考文献」「附录」标题已自动设章级大纲级别（`outlineLvl`=0），F9 刷新后必进目录**——解决"正文有附录、目录却列不出"的问题。自检会打印**各节中文字数**，据此对照 `references/篇幅档位.md` 看哪节欠字再补；还会查**未填占位**（`〈〉`/`TODO`/`待补`/`XXX`，正文、参考文献、封面分开报）——**照抄示例 json 时封面 `〈XXX〉` 和占位参考文献最容易漏，见到警告必须清掉再交付**；**自查会打印「行距: 通过/⚠️」**（三级标题单倍+段前段后各0.5行、正文1.5倍、表图题单倍，自动扫正文区逐段核验，⚠️ 按报的段头改，见 `references/格式规范.md`「页面」）。生成时会**自动把目录区/正文区/文末的连续空段折叠为 1 个**（**封面区整体不动**——banner 到题目、题目到封面表格的垂直定位空段保留原样；分节符 SECT 段由脚本保护、不被折叠或误删），消除"封面和目录之间像夹了空页/一堆空行"的观感。**参考文献表除查占位外，还须按 `references/参考文献著录.md`「写入前收尾核查」对每条目过一遍（末尾结束符/全角标点/作者数/页码/序号连续）——这条是防"16 条全带句号被打回"这类事故的闸门，务必执行**。依赖 `lxml`（缺则脚本会报错并提示 `pip install lxml`）。
   > **文档属性自动去个人信息**：生成 docx/pptx 时会清掉模板 `docProps` 里的作者姓名、邮箱、单位、最后修改人、上次打印时间与批注作者，并把作者写成 `cover.作者姓名`（仍是 `〈XXX〉` 占位则留空）。内置模板本身已清理过一遍。**用户自带学院模板时这层照样生效**——那类模板常留着教务老师的姓名邮箱，不清就会跟着你的稿子一起交上去。
   > 若用户提供了**新的 .docx 模板**（如学院自定版），用 `--template` 指向它即可，脚本按文本定位通用。
6. **自查并回显结果**：按 `references/自查清单.md`（7 项必备 + 逐节质量闸 + 篇幅 + 格式 + 评审标准 + 查重提示）逐条核对，可对照 `templates/范例-问题研究型.md`。**核对完成后，把自查结果直接输出给用户看**（markdown 呈现，不只在内部自评、也不仅一句"已自查"）：
   - 逐条列出每项的「✅ 通过 / ⚠️ 需改（原因）」；
   - 结尾给出一行**结论**：「✅ 全部通过，可交付」或「⚠️ 存在 N 项需修改，清单如下」；
   - 对 ⚠️ 项给出**具体修改建议**，并主动询问用户是否要据此修订再复查。
7. **一站式收尾（自查通过后主动询问，全部可选）**：用 **AskUserQuestion 多选**问用户还需要哪些交付物——**汇报 PPT**（三套内置北航答辩模板可选）/ **汇报演讲稿** / **答辩问答预测** / **多格式导出**，并明确告知"都不需要也可以，直接结束"。四项的具体做法、PPT 依赖前置检查与降级分支，见 `references/一站式收尾.md`。**PPT 内容生成有密度要求**（页数/每章分配/每页 bullets/图表占位），写 `ppt_content.json` 前必读一站式收尾.md 的「PPT 内容生成规范」。用户选了哪几项就做哪几项，逐项交付并给出文件路径；未选的不做、不追问第二遍。
8. **查重提示**：提醒阈值与送检要求。
> 需要精确原文时直接 Read 工作目录下对应原始文件（见索引），勿凭记忆。

## 参考资料（skill 内置）
- `references/选题引导.md` — 用户"没想好"时的**三轮固定提问脚本** + 候选卡六栏格式 + 必须给推荐意见 + **题目太大先拆** + **大纲自查四问**（已吸收 brainstorming 核心，无需该技能）
- `references/问卷与访谈提纲.md` — **拟问卷 / 访谈提纲**（何时主动问、量表与题项设计、逐题必查六条、量表排成表格放进 appendix 的写法；开题阶段只写"拟发放"、不编数据）
- `references/文献检索.md` — 联网检索真实文献→整理 GB/T 7714→支撑文献综述的完整流程
- `references/篇幅档位.md` — 精简/标准/充实三档 + 自定义字数的每节字数控制
- `references/参考文献著录.md` — GB/T 7714 著录规范
- `references/论文类型详解.md` — 四类论文结构/写作要点/范文
- `references/格式规范.md` — 字体字号、页面等格式
- `references/去AI化写作守则.md` — 中强度去AI化写作守则（**写入时执行**，非事后润色）
- `references/自查清单.md` — 输出前逐条核对清单（可勾选）
- `references/审阅已有稿.md` — **审阅/修订已有稿**流程（用户已有一份报告时走这条，不走引导选题）
- `references/一站式收尾.md` — 自查通过后的可选交付物（PPT / 演讲稿 / 答辩问答 / 多格式导出）
- `templates/开题报告大纲.md` — 内容草稿脚手架；`templates/范例-问题研究型.md` — 参考范文
- `templates/内容示例-社区减负.json`（问题研究型，**含表格/插图/列表/脚注四种混排块示例**）、`内容示例-案例分析.json`、`内容示例-调研分析.json`、`内容示例-政策分析.json` — content.json 格式示例（四类各一）
- `assets/templates/ppt/模板1-北航答辩通用.pptx`、`模板2-…`、`模板3-…` — 三套内置答辩 PPT 模板（16:9，生成 PPT 时让用户选）
- `scripts/build_ppt_from_template.py` — 基于上述模板生成汇报 PPT（改而不建，自动清水印/占位）
- `scripts/build_from_template.py` — 基于官方 Word 模板生成开题报告 .docx（支持正文段 + 三线表/插图/自动编号列表/脚注混排，图表题自动编号，可自动新建「五、预期目标和成果」节）
- `scripts/make_route_figure.py` — 生成技术路线图 PNG（Pillow + 系统宋体，零新增依赖；出图后用 image 块嵌入）
- `scripts/read_docx.py` — 读已有开题报告 .docx 并按节切分（审阅路径用；只读不改原文件；会报缺失节、缺「预期目标和成果」、以及提了问卷/访谈却无附录）
- `references/ars-imports/` — 原样导入的 **ARS 学术技能包**方法论（CC BY-NC 4.0），清单与用途见 `references/ars-imports/内置说明.md`
  - `source_quality_hierarchy.md` — 来源质量分级（含社科/政策分级、掠夺期刊红旗）
  - `literature_matrix_template.md` — 文献矩阵（来源×主题交叉表，综述归纳利器）
  - `literature_monitoring_strategies.md` — 文献跟踪策略
  - `openalex_api_protocol.md` — OpenAlex 协议细则（匹配阈值/速率/退化）
  - `crossref_api_protocol.md` — Crossref 协议细则

## 原始素材索引（选读）
> 本 skill **自带全部必需文件**（模板、PPT 母版、规范条文都已固化在 `assets/` 与 `references/`），下列原始素材**没有也能正常跑**。若用户工作目录里恰好有这些文件，可读来核对最新版；**路径以用户实际所在目录为准，不要假定固定位置**。
- `开题报告格式2025.9.doc` / `.docx` — 官方模板。skill 内置副本：`assets/templates/开题报告模板.docx`（生成时一律用内置副本，**勿改用户原文件**）。读取结构：`textutil -convert txt -stdout "开题报告格式2025.9.doc"`
- `研究生学位开题报告管理办法.pdf` — 北航校字〔2021〕40号。读取：`pdftotext -enc UTF-8 "研究生学位开题报告管理办法.pdf" -`
- `北京航空航天大学研究生撰写学位论文的规定.docx` — 撰写规定全本。读取：`textutil -convert txt -stdout "..."`
- `学术不端文献检测系统使用规定.docx` — 查重阈值。
- `2026春季第一讲.pdf` — MPA 论文基本概述/要求/程序/学术规范。
- `2026春季第二讲-论文类型.pdf` — 四种论文类型结构与范文。
